---
name: Bug report
about: Something does not work as described
title: ''
labels: bug
assignees: ''
---

## What happened

<!-- What you observed. -->

## What you expected

## Command you ran

```
python main.py ...
```

## Output

<!-- Paste the terminal output, including the summary line
     (tested=... | workers=... | elapsed=...).
     Remove any proxy addresses you would rather not publish. -->

```
```

## Environment

- OS:
- Python version (`python --version`):
- Proxy Tester version (`python main.py --version`):
- `aiohttp` and `python-socks` versions (`pip show aiohttp python-socks`):

## Notes

<!-- Does it happen every run or intermittently?
     If a specific source or proxy is involved, name it.
     "No proxies were found" is often a network or firewall issue: please say
     whether `python main.py --list-sources` works and whether you are behind a
     corporate proxy or VPN. -->
